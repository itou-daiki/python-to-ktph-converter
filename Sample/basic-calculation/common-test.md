# 四則演算

難易度: 基礎
学習ポイント: 整数入力、四則演算、ゼロ除算の回避

```
# 二つの整数を使った計算
a = 【外部からの入力】
b = 【外部からの入力】

表示する("和: " + 文字列(a + b))
表示する("差: " + 文字列(a - b))
表示する("積: " + 文字列(a * b))

もし b != 0 ならば:
｜ 表示する("商: " + 文字列(a ÷ b))
｜ 表示する("余り: " + 文字列(a ％ b))
そうでなければ:
⎿ 表示する("0では割れません")
```
