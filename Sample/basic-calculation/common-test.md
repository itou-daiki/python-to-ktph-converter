# 基本計算

入力と出力、四則演算の基本

```
# 基本的な計算のサンプル
表示する("二つの数を入力してください")
a = 【外部からの入力】
b = 【外部からの入力】

表示する("a = " + 文字列(a) + ", b = " + 文字列(b))
表示する("和: " + 文字列(a + b))
表示する("差: " + 文字列(a - b))
表示する("積: " + 文字列(a * b))
表示する("商: " + 文字列(a ÷ b))
表示する("余り: " + 文字列(a ％ b))
```